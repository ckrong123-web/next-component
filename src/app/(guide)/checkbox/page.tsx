'use client';

import React from 'react';
import { Guide, CheckboxGuide } from '@/guide';

export default function CheckboxGuidePage() {
    return (
        <Guide title="체크박스 컴포넌트">
            <CheckboxGuide />
        </Guide>
    );
}
